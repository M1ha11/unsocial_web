<!DOCTYPE html>
<html lang="en">
  <%= render 'layouts/head' %>
  <body>
    <%= render 'layouts/header' %>
    <div class="container">
      <div class="row">
          <%= render 'layouts/error' %>
          <%= yield %>
      </div> <!--/row-->
      <%= render 'layouts/footer' %>
      <%= debug(params) if Rails.env.development? %>
    </div> <!-- /container -->
  </body>
</html>
