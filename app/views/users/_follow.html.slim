= form_for(current_user.active_interrelationship.build(followed_id: @user.id), remote: true) do |f|
  div
    = hidden_field_tag :followed_id, @user.id
    = f.submit "Follow", class: "btn btn-primary"
